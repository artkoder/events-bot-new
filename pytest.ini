[pytest]
testpaths =
    tests

addopts =
    -m "not e2e"

norecursedirs =
    3d_intro
    scripts

markers =
    smoke: Tests covering critical smoke scenarios.
    asyncio: Mark a test as asyncio-based to run with the event loop.
    e2e: Tests requiring live Telegram bot/user credentials.
