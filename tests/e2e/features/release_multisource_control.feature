# language: ru
Функция: Контрольные multi-source события (VK + TG + /parse) для релиза

  Предыстория:
    Дано я авторизован в клиенте Telethon
    И я открыл чат с ботом
    Когда я отправляю команду "/start"

  # Этот набор сценариев предназначен для финальной проверки релиза:
  # - либо запускается после `release_smoke_smart_update.feature`,
  # - либо отдельно, если вы уже прогоняли VK/TG/parse и в БД есть свежие результаты.

  @manual
  Сценарий: В БД есть хотя бы одно событие, смерженное из VK + TG + parser
    Тогда существует событие с источниками VK, Telegram и парсером
    Когда я запрашиваю /log для события с источниками VK, Telegram и парсером
    Тогда я вижу лог источников с датой, временем, источником и фактами
    И в логе источников есть источники VK и Telegram
    И в логе источников есть источник "parser:"

  @manual
  Сценарий: Контрольные события (заполните список под дату релиза)
    # Заполните 3–5 строк контрольных кейсов (будущие события), которые точно присутствуют в трёх источниках:
    # - VK пост (vk_inbox)
    # - Telegram пост (monitoring)
    # - /parse (site/parser)
    #
    # Рекомендованный процесс подготовки описан в `docs/operations/release-smoke-smart-update.md`.
    Тогда я логирую полный текст сообщения
