# Туристическая метка (ручной режим)

Фича позволяет редакторам вручную помечать события как «🌍 Туристам интересно/неинтересно»,
выбирать причины из словаря и оставлять короткий комментарий. Выгрузка результатов делается
через `/tourist_export` в `.jsonl` для дальнейшего анализа.

## Документы

- `docs/features/tourist-label/user-story.md` — пользовательская история и UX требования.
- `docs/features/tourist-label/research.md` — цели исследования и формат данных.

## Кодовые entrypoints

- `main.py` — inline-кнопки, сессии меню причин/комментария, обработчик callback `tourist:*`.
- `db.py` — поля `tourist_*` в таблице `event` и индексы.

## Тесты

- `tests/test_tourist_features.py` — базовая проверка клавиатуры и сборки карточки.

