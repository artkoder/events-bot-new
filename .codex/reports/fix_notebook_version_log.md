Updated the engine log string to `v24.15-fix-mask` in `kaggle/VideoAfisha/video_afisha.ipynb` and verified there are no `fl_image` references in that notebook, so there was nothing to remove. Committed on the `dev` branch.

Next steps:
1) `git push origin dev`