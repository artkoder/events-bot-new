**Findings**
- High — парсинг даты в `scrape_tretyakov_detail` был слишком хрупким: `\w+` и строгий пробел ломались на «6 февраля, в 20:00», из‑за чего детальная дата не находилась и логика откатывалась к общему виджету (риск повторения критического бага). Исправил: русские месяцы + опциональная запятая, выбор первой не‑прошедшей даты. `scripts/update_tretyakov_nb.py:137`
- Medium — в ветке с detail‑датой цена/статус искались только по дате и стартовали как `available`, поэтому при нескольких слотах на одной дате могла прилетать цена не того времени и статус «available» без подтверждения. Исправил: матч по дате+времени с нормализацией, дефолт `unknown`. `scripts/update_tretyakov_nb.py:348`

**Assumptions**
- Предполагаю, что на detail‑странице формат остаётся «… в HH:MM». Если встречаются «6 февраля 20:00» или с годом, стоит расширить шаблон.

Готов к тестовому запуску; тесты не запускал из‑за отсутствия сетевого доступа в среде.

Следующие шаги (если нужно):
1. `python scripts/update_tretyakov_nb.py` — обновить код в ноутбуке из скрипта.
2. `python debug_pianissimo.py` — точечная проверка Pianissimo (нужен сетевой доступ).