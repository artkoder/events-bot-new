Проверил логику загрузки и источник ноутбука — сейчас это определяется только `kernel_ref`.

Как бот загружает ноутбук на Kaggle
- Поток: `_render_and_notify` → `_push_kernel` → `KaggleClient.deploy_kernel_update`, где и происходит загрузка в Kaggle через `api.kernels_push`. См. `video_announce/scenario.py:1888` и `video_announce/scenario.py:2745`, `video_announce/kaggle_client.py:190`.
- В `deploy_kernel_update` есть два режима: локальный и удалённый. См. `video_announce/kaggle_client.py:190`.

Используется репозиторий или скачивание с Kaggle
- Если `kernel_ref` начинается с `local:`, бот копирует папку из репо `kaggle/<Folder>` и пушит её в Kaggle. См. `video_announce/kaggle_client.py:199`.
- Если `kernel_ref` НЕ начинается с `local:`, бот сначала **скачивает kernel с Kaggle** (`api.kernels_pull`), потом пушит его обратно с обновлёнными dataset_sources. См. `video_announce/kaggle_client.py:225`.
- Выбор источника задаётся в UI: локальные kernel’ы с 📦 и kaggle-овые с 📓. См. `video_announce/scenario.py:2341`.

Почему старый код всё ещё используется
- Если в сессии сохранён `kernel_ref` без `local:` (например, `username/video-afisha`), то бот **тянет код с Kaggle**, а не из репозитория. Это ровно ваш симптом: изменения в `kaggle/VideoAfisha/video_afisha.ipynb` не попадают.
- Дополнительно: после каждого пуша `kernel_ref` перезаписывается на ID из metadata (обычно `username/video-afisha`), даже если запуск был локальный. См. `video_announce/scenario.py:1900` и `video_announce/kaggle_client.py:240`. Это значит, что при повторном запуске сессии код уже берётся с Kaggle.

Что это означает для задачи
- Если в текущей сессии `kernel_ref` — Kaggle-овый (`user/slug`), **нужно обновить kernel на Kaggle** или выбрать локальный `📦` kernel заново.
- Если хотите гарантированно использовать репозиторий, при запуске выбирайте `📦` (локальный) kernel, где `ref = local:VideoAfisha` (берётся из `kaggle/VideoAfisha`). См. `video_announce/scenario.py:2347`.

Если нужно, могу подсказать как быстро проверить текущий `kernel_ref` в базе/логах и убедиться, что он `local:`.