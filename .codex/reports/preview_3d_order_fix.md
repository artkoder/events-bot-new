Убрал рандомизацию порядка изображений и сделал slug датасета зависящим от session_id, чтобы параллельные /3di не перетирали друг друга.

**Отчет**
- `kaggle/Preview3D/preview_3d.ipynb`: удалил `random.shuffle(files)`, порядок теперь строго `sorted()`.
- `preview_3d/handlers.py`: slug датасета формируется как `preview3d-{session_id}` (через новый префикс).

Тесты не запускал (не просили). Если нужно, могу запустить.