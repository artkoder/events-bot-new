**Выводы**
- `ics_publish` проваливается в 49.3% (727 expired) → процессы не завершаются в срок: таймауты или нет потребителей/доступа к внешнему сервису.
- `month_pages` и `vk_sync` имеют заметные expired/captcha, но уровень успеха 90–98%—локальные проблемы.
- >100 попыток — вероятно бесконечные ретраи без backoff/идемпотентности; такие задачи могли “закиснуть” и не вычищаются.
- 8313 задач без повторов → базовый поток стабильный; сбой точечно в `ics_publish`.

**Ответы на вопросы**
1) `ics_publish`: массовые expired → либо задача блокируется внешними зависимостями (ICS публикация/вебхук), либо таймаут слишком мал для типичной длительности, либо воркеры не успевают из-за узкого пула/блокировок.
2) >100 попыток: зациклившиеся задания (неправильный статус после фейла, авто-ретраи без лимита/Dead Letter Queue), шумят и могут захламлять очередь.
3) Рекомендации по надёжности:
   - Для `ics_publish`: поднять или адаптивно выставлять таймаут; добавить идемпотентные операции и внешний health-check ICS-эндпоинтов; ввести DLQ с лимитом попыток (например, 5–8) и алерты; включить измерение времени выполнения и распределение латентности.
   - Для капч в `vk_sync`: внедрить анти-капча/ручной фоллбек; уменьшить агрессивность запросов/ротацию токенов.
   - Общие: экспоненциальный backoff, джиттер, лимит попыток; метрики “age in queue” и “attempts”; авто-пауза типов задач при всплеске ошибок; перезапуск или скейл воркеров, шардирование по типам.
   - Очистка зацикленных задач: регулярный скан >N попыток → DLQ/ручная обработка; мониторинг “expired by task type”.
4) Переход на отложенные ребилды: риск главным образом в `ics_publish` (высокий expired). Остальные задачи стабильны, но до включения отложенных ребилдов нужно снизить expired на `ics_publish` и включить лимит попыток+DLQ, чтобы отложенные задания не залипали.