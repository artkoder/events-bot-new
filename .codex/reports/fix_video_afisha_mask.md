Исправил маску MoviePy: убрал расширение до 3D, оставил 2D и уточнил комментарий; добавил запись в changelog и закоммитил в `dev`.

**Phases**
- Phase 1: Нашел `rgba_image_to_clip` в `kaggle/VideoAfisha/video_afisha.ipynb` (cell 6); результат: точка правки подтверждена.
- Phase 2: Удалил `fl_image` и оставил маску 2D с поясняющим комментарием в `kaggle/VideoAfisha/video_afisha.ipynb`; результат: функция соответствует ожидаемому поведению MoviePy.
- Phase 3: Добавил запись в `CHANGELOG.md` и сделал коммит `d637b42`; результат: изменения зафиксированы в ветке `dev`.

Тесты не запускал (ноутбук не выполнял).

Если нужно продолжить:
1) Перезапустить ячейку с `rgba_image_to_clip` в Kaggle и проверить рендер.
2) Прогнать полный рендер видео для подтверждения отсутствия `ValueError`.